# ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Neovim aliases
alias nv='nvim'
alias nvw='nvim .'

# Git aliases
alias g='git'

# Reload zsh
alias src="source $HOME/.zshrc"

# Kill Konsole
alias kk="killall konsole"

